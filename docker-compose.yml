version: '3.8'
services:
  yjs-server:
    image: node:18-alpine
    command: sh -c "npx --yes y-websocket"
    environment:
      - PORT=1234
      - HOST=0.0.0.0
    ports:
      - "1234:1234"
    restart: unless-stopped