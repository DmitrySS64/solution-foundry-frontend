# TODO

## Базовая функциональность графического редактора

1. Drag & drop с сайдбара ✅
   - Реализовать протаскивание/перенос объектов из сайдбара на холст.
   - Поддержать создание нового объекта при drop.
   - Acceptance criteria:
     - При перетаскивании из сайдбара на canvas объект появляется.
     - Координаты drop корректно учитывают viewport/панорамирование/масштаб.
   - Статус:
     - `DiagramSidebar` пишет `application/diagram-node-type` в `dataTransfer`.
     - `DiagramCanvas.handleDrop()` переводит pointer в world coordinates и вызывает `createNode()`.

2. Фигура создается в центре viewport'а ✅
   - Для создаваемых объектов (из sidebar/из нотаций) определить поведение: по умолчанию позиционировать в центре текущего viewport.
   - Acceptance criteria:
     - Создание фигуры без указания позиции приводит к появлению в центре видимой области.
     - При панорамировании/зумировании центр считается корректно.
   - Статус:
     - Click по элементу `DiagramSidebar` создает узел в центре viewport.
     - `DiagramToolbar` создает rectangle в центре viewport.

3. Объекты из нотаций могут быть примитивами/картинками/составными фигурами ✅
   - Реализовать модель, в которой нотация может отдавать:
     - примитив
     - картинку/svg
     - составную фигуру из примитивов
   - Применить настройки оформления на все части составной фигуры.
   - Acceptance criteria:
     - Для составной фигуры отдельные примитивы визуально отображаются как единая сущность.
     - Стили/оформление задаются на уровне объекта и корректно распространяются на компоненты.
   - Статус:
     - `NodePrimitive` поддерживает `rect`, `circle`, `diamond`, `text`, `svg`.
     - `ShapeRenderer` рендерит primitives и `node.notation.svg`.
     - `textKey` связывает text primitive со свойством нотации.

4. У объекта может быть кастомная форма (компонент/технология/описание) ✅ базовый контракт
   - Поддержать рендер кастомной формы вместо стандартного рендера примитива/состава.
   - Обеспечить единый контракт данных: какие props/поля передаются в кастомный компонент.
   - Acceptance criteria:
     - Можно подключить кастомный компонент для отображения объекта.
     - Кастомная форма корректно масштабируется/располагается на холсте как обычный объект.
   - Статус:
     - Есть `customRendererRegistry`.
     - `ShapeRenderer` выбирает renderer через `node.customRendererId`.
     - Текущий контракт: `{ node: DiagramNode }`.

## Страницы (вкладки) в пределах одного документа

5. Сделать страницы (вкладки в пределах одного документа) ⏳ не реализовано
   - Реализовать переключение между страницами в рамках одного проекта/документа.
   - Определить хранение state: какие объекты относятся к какой странице.
   - Acceptance criteria:
     - Добавление/переключение страниц работает без потери данных.
     - Отображение холста соответствует выбранной странице.
     - Сохранение/загрузка документа сохраняет разбиение по страницам.
   - Нужно:
     - Расширить `DiagramDocument` до pages-based модели.
     - Добавить `activePageId`.
     - Обновить serializer/deserializer.
     - Добавить UI вкладок.

## Реализация нотаций и кастомных стрелок

6. Реализовать следующие нотации: BPMN, UML, C4 ✅ для узлов / ⏳ для edge tools
   - Acceptance criteria:
     - Для каждой нотации доступен набор элементов.
     - Элементы нотации рендерятся корректно по определенным правилам.
   - Статус:
     - `public/notations/bpmn.json`, `uml.json`, `c4.json` подключены.
     - `notationRegistry` маппит элементы в node definitions.
     - Sidebar группирует элементы по `base`, `bpmn`, `uml`, `c4`.
   - Осталось:
     - Разделить node definitions и edge definitions.
     - Не маппить элементы `extends: "edge"` как узлы.
     - Использовать `defaults.style` из JSON-нотаций при создании node/edge.

7. Для нотаций реализовать кастомные стрелки отдельным объектом в декларации ⏳ частичная заготовка
   - Поддержать определение связи/стрелки как отдельного типа графического объекта в декларации нотации.
   - Для примера: “вороньи лапки” у связей нотации Мартина как заданная форма концов/маркер.
   - Acceptance criteria:
     - Создание и рендер связей использует кастомную геометрию/маркеры.
     - Кастомная стрелка корректно привязывается к источнику/приемнику и пересчитывает точки при изменениях.
   - Статус:
     - `EdgeEndMarkerRegistry.tsx` содержит `arrow` и `c4.crow_feet`.
     - `edgeMarkerGeometry.ts` умеет вычислять позицию/поворот маркера.
     - `EdgeEndStyle` пока ограничен `none | arrow`.
     - `EdgeRenderer` пока использует встроенный Konva `Arrow`, а не marker registry.
   - Нужно:
     - Расширить `EdgeEndStyle` до marker id или отдельного marker object.
     - Подключить `getEdgeEndMarker()` в `EdgeRenderer`.
     - Добавить декларативные edge definitions из JSON.

## Документация

8. Обновить STRUCT.md ✅
   - В главе "Архитектура графического редактора" описать:
     - модель данных
     - систему нотаций
     - принцип архитектуры редактора для отчета
   - Acceptance criteria:
     - В разделе есть четкая структура: данные, нотации, архитектура.
     - Описано как декларативно задаются нотации/фигуры/стрелки и как это используется в редакторе.
   - Статус:
     - `docs/STRUCT.md` обновлен.
     - Контекст разнесен по `docs/diagram-editor/`.

## Ближайшие технические задачи

9. Реализовать pages model
   - Предлагаемый формат:
     - `DiagramDocument.pages: DiagramPage[]`
     - `DiagramDocument.activePageId?: string`
     - `DiagramPage: { id; name; nodes; edges }`

10. Вынести декларативные edge definitions
    - Добавить `edgeDefinitions.ts`.
    - Поддержать элементы JSON с `extends: "edge"`.
    - Создавать edge из выбранного edge tool.

11. Подключить кастомные edge markers
    - Использовать `EdgeEndMarkerRegistry`.
    - Поддержать marker id в инспекторе.
    - Сохранить обратную совместимость `none`/`arrow`.

12. Доработать selection box
    - Сейчас selection box визуальный.
    - Нужно выбрать все nodes/edges, пересекающиеся с прямоугольником.

13. Реализовать или скрыть `bezier`
    - Тип есть в модели и инспекторе.
    - Геометрия bezier отдельно не реализована.

14. Эдит диалог
15. Свойство "блокировка", не дающие объекту менять позицию и растягивать
16. Контекстное меню на холст
17. Коприрование при зажатом Ctrl и перетаскивании
18. В сайдбаре переделать базовое поле label с input-text на area, т.к. содержимое может быть не в одну строку. Добавить в конструкторе тип text-area
19. Починить выделение
20. Добавить групповое выделение:
    1.  Групповое перетаскивание
    2.  Масштабирование (трансфорер) работает только по отдельности
21. Совместная работа (CRDT: Yjs + WebSocket):
    1.  В графическом редакторе 
    2.  В текстовом редакторе
